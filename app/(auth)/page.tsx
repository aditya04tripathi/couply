import React from "react";
import RegisterPageClient from "./page-client";

export const metadata = {
  title: "Register | Couply",
};

const RegisterPage = () => {
  return <RegisterPageClient />;
};

export default RegisterPage;
